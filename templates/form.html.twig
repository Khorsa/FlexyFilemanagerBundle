<div class="row article-form-image">
    <div class="col-auto">
        <img src="{{ flexycms.uploads_path }}/{{ image.vars.value.name }}" />

    </div>
    <div class="col">
        {{ form_row(form.image) }}
    </div>
</div>

